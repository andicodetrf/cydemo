### initiate server

`cd server && node .`


#
### initiate client

`cd client && npm start`
<br/>

Open [http://localhost:3000](http://localhost:3000) to view it in the browser.


#
### initiate cypress

`cd client && npx cypress open`